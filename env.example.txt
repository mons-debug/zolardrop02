# Database Configuration
# For production: PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/zolar?schema=public"

# For local development with SQLite (optional)
# DATABASE_URL="file:./dev.db"

# JWT Authentication
# Generate a strong random secret for production
# You can use: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Pusher (Real-time Notifications)
# Get credentials from: https://dashboard.pusher.com/
PUSHER_APP_ID="your-pusher-app-id"
PUSHER_KEY="your-pusher-key"
PUSHER_SECRET="your-pusher-secret"
PUSHER_CLUSTER="your-cluster"

# Public Pusher Keys (exposed to client)
NEXT_PUBLIC_PUSHER_KEY="your-pusher-key"
NEXT_PUBLIC_PUSHER_CLUSTER="your-cluster"

# Google Analytics 4 (Optional)
# Get from: https://analytics.google.com/
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# Hotjar Analytics (Optional)
# Get from: https://insights.hotjar.com/
NEXT_PUBLIC_HOTJAR_ID="1234567"

# Legacy Admin Token (for backward compatibility)
# New authentication uses JWT, this is optional
ADMIN_TOKEN="admin-token-123"

# Production URLs
# NEXT_PUBLIC_SITE_URL="https://yourdomain.com"

# Email Configuration (if implementing email features)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASSWORD="your-app-password"
# SMTP_FROM="noreply@yourdomain.com"

INSTRUCTIONS:
============
1. Rename this file to .env in your project root
2. Replace all placeholder values with your actual credentials
3. Never commit the .env file to version control
4. For Vercel deployment, add these variables in the Vercel dashboard

